<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta http-equiv="X-UA-Compatible" content="ie=edge">
        {{ style('css/app.css') }}
    </head>
    <body>
        <header>
            <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
                <a class="navbar-brand" href="#">Navbar</a>
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNavDropdown">
                    <ul class="navbar-nav">
                        <li class="nav-item active">
                            <a class="nav-link" href="{{ route('layout.edge')}}">{{antl.formatMessage('nav.home')}}</a>
                        </li>
                        @loggedIn
                        <li class="nav-item">
                            <a class="nav-link" href="{{ route('logout')}}">{{antl.formatMessage('nav.logout')}}</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ route('userShow')}}">Profile</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ route('websocket')}}">{{antl.formatMessage('nav.websocket')}}</a>
                        </li>
                        @else
                        <li class="nav-item">
                            <a class="nav-link" href="{{ route('login_view')}}">{{antl.formatMessage('nav.login')}}</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ route('signup_view')}}">{{antl.formatMessage('nav.signup')}}</a>
                        </li>
                        @endloggedIn

                        <li class="nav-item">
                            <a class="nav-link" href="{{ route('addManga')}}">{{antl.formatMessage('nav.add_manga')}}</a>
                        </li>
                    </ul>
                </div>
            </nav>
        </header>
        <div class="container">
            @!section('content')
        </div>
        {{ script('js/app.js') }}
        @!section('script')
    </body>
</html>
